{
  "defaultSeverity": "error",
  "extends": ["tslint-config-standard", "tslint-eslint-rules"],
  "globals": {
    "require": true
  },
  "linterOptions": {
    "exclude": [
      "node_modules/**"
    ]
  },
  "rules": {
    "no-angle-bracket-type-assertion": false, // 类型断言必须使用 as Type，禁止使用 <Type>
    "no-empty": false,
    "no-debugger": false,
    "no-for-in-array": false,
    "whitespace": false,
    "no-trailing-whitespace": false,
    "no-consecutive-blank-lines": [true, 3], // 禁止连续超过三行空行
    "await-promise": false,
    "deprecation": false,
    "no-floating-promises": false,
    "no-unnecessary-qualifier": false,
    "no-unnecessary-type-assertion": false,
    "strict-type-predicates": false,
    "space-before-function-paren": false, // 函数名前必须有空格,false
    "triple-equals": [true, "allow-null-check"], // 必须使用 === 或 !==，禁止使用 == 或 !=，与 null 比较时除外
    "trailing-comma": false, // 禁止或强制使用尾随逗号
    "semicolon": false, // 行尾必须有分号
    // "no-var-requires": true, // 不允许使用var module = require("module"),用 import foo = require('foo')导入
    "quotemark": false, // 必须使用单引号,false
    "radix": false // parseInt 必须传入第二个参数
  }
}